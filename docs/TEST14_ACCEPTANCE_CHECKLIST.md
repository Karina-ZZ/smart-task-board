# Test14 本地最终验收清单

> 只接受Test14新解压目录的证据。测试结果必须记录执行环境、时间、命令、返回码和失败项。此清单本身不代表测试已经执行。

## A. 版本和保护
- 计算累计ZIP的SHA-256并与外置TEST14_SHA256.txt核对。
- 前后端进程目录都为smart-task-board-test14；确认环境文件、数据库指向。
- 执行 `python scripts/verify-test14-scope.py`，白名单外变更与删除必须为0。
- app/models、alembic和小程序生产文件应与Test13一致；数据库实际schema快照前后核对，不新增表、字段、索引、约束。
- 不清理已有业务/demo数据，不修改原有授权范围、身份或任务状态以“修复”测试。

## B. F1强制触发
使用真实创建人账户，通过真实API发送本轮唯一任务，使其进入pending_acceptance。
工作台摘要、任务列表、Inbox和available-actions均不得500。
不能只断言200：摘要/列表必须包含刚发送任务的task_id，创建人可用动作包含reassign_task。
承办人不自动获得reassign_task；无关人员不能看到该任务；越权转派返回403且任务版本不变。
Web真实登录后工作台显示此任务，进入详情、返回、刷新、重新访问login均正常。
保留观察者对照，但观察者通过不能替代创建人通过。

## C. F2输入与落库
非法周期“每周”、weekly、非法星期、24:00/09:60等，创建和更新均返回422。
非法创建无任务/参与人/节点/状态日志/通知业务残留；非法更新不改变原值和任务版本。
合法完整周期正常保存；null合法；更新省略字段保留原值，显式null清空。
9项必填缺失仍按原规则阻断，汇报周期不能成为新增必填。

## D. F3及AI非硬门槛
有明确星期和时间时才允许完整周期；只有“每周”时不猜默认时间。
非法AI建议不得原样入库；可选周期提示不得新增发送硬门槛。
用户已明确输入的有效周期不被非法AI建议覆盖，历史识别证据不被重写。
真实ChatService/Qwen联通单独记录；固定模型返回只标固定输入测试。

## E. 小程序API页面
开发者工具须使用API模式而非mock，实际执行：
AI仍有提示 → 不回答AI → 手工补齐9项 → 来源留空 → 保存 → 最终发送 → 返回详情/工作台。
记录保存、发送、重新读取的请求，以及task_id/版本/状态。确认未发送追问回答请求，未产生正式节点/依赖；待接受通知仅给承办人。
缺服务时应真实报错，不得回落到mock并宣称成功。

## F. 原有回归
原28项PG及新增4项，共32项连续3轮；原Outbox5×20全部通过。
原510项非PG及本轮新增58项均保留；本容器568项通过仍需Python3.12复验。
小程序22项、50个JS语法、ChatService3项、Ruff、完整Web lint/Vitest/build。
Web新增1项契约单测和真实创建人工作台用例；原115项Web测试不得删除或降低断言。
原接受→拆解→执行→汇报/卡点→变更→完成→验收归档、高管/通知等用例保持。

## G. 打包与结论
最终ZIP重新解压到新目录复验，不复用开发工作树；日志、请求、截图分享前脱敏。
技术门禁、真实HTTP/Web、小程序API页面、真实模型、正式WeCom分别给结果。
未执行项标PENDING/BLOCKED，不能用Test13结果或单元测试代替Test14真实结果。
